{
  "name": "card-game",
  "version": "0.1.0",
  "private": true,
  "description": "Card game frontend with future Spring Boot backend",
  "scripts": {
    "lint": "eslint src tests",
    "test": "jest --runInBand",
    "test:ci": "jest --ci --coverage"
  },
  "devDependencies": {
    "eslint": "^9.24.0",
    "@eslint/js": "^9.24.0",
    "jest": "^29.7.0",
    "globals": "^15.15.0"
  }
}
